-- Application schema is managed by Prisma migrations from app/backend/prisma.
-- Keep this file intentionally minimal so `prisma migrate deploy` can run
-- against an empty database on first startup.
CREATE EXTENSION IF NOT EXISTS pgcrypto;
